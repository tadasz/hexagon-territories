# Downloaded model weights live here; they are verified against models/manifest.json
# and committed only as git-lfs pointers under apps/ios/Resources/Models by Stream E.
models/cache/
__pycache__/
*.pyc
