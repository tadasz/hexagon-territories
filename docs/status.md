# Feature status — 001 to 004

Snapshot written by feature 004's integration stream on 2026-09-07 (branch `claude/nature-explorer-app-plan-h17bxs`, working tree over `815612f`). Every number below was produced in a Linux container (Ubuntu 24.04, Node 22.22.2, pnpm 10.33.0, Postgres 16.13 + PostGIS 3 without h3-pg, Swift 6.2.1 Linux toolchain; no Docker daemon, no macOS). The per-feature detail lives in each `specs/NNN-name/{tasks.md,analysis.md,quickstart.md}`; `apps/ios/VERIFY.md` keeps the Swift record.

## Features

| Feature | Spec status | `/speckit-converge` verdict | Tests verified in this container | Open tasks (need macOS / Docker / owner) |
|---|---|---|---|---|
| **001 repo-foundations** | Draft (becomes Implemented when T048 closes) | Not yet Converged — `tasks_appended` (only environment/owner items remain) | `pnpm lint`, `typecheck`, `test`, `test:db`; `swift test` H3Kit 21, TerritoryRules 29; `python3 -m unittest` 31 (re-run today: territory-rules 233, h3-fixtures 12, ml 31) | T048 converge (after the rest); T049 git-lfs model weights; T050 macOS app target; T051 Xcode Cloud (owner); T052 first Docker `make dev`/`make test` + CI run; T053 licence inquiries (owner) |
| **002 auth-and-factions** | Implemented on Linux | Not yet Converged — `tasks_appended` (macOS, Docker/MinIO, owner, first CI run) | API 140 DB tests / 91 unit; `@nature/api-schema` 3; Swift Core 66, APIClient 13, AuthFeature 6, FactionsFeature 9, ProfileFeature 13, DesignSystem 1 (today: api-schema 3, Core 79, APIClient 21 — the later features' supersets) | T031 converge (after the rest); T032 macOS/simulator; T033 Sign in with Apple on the App ID, `DEVELOPMENT_TEAM`, `APPLE_CLIENT_IDS`, `JWT_SECRET` (owner); T034 Docker/MinIO storage + export; T035 first CI run incl. the `swift` job |
| **003 walk-tracking** | Implemented (owner/macOS follow-ups) | Converged for everything buildable on Linux; T031–T035 appended | walk-sim 42 (today 46), API 121 unit / 345 DB (today 201 / 345 incl. 004), Swift Core 79, Location 28, Persistence 31, WalkFeature 24, APIClient 21 | T031 usage-string copy (owner); T032 battery on a device (owner); T033 SC-003 timings on a device; T034 `xcodegen` + `xcodebuild test` + simulator flows (macOS); T035 Docker `make test` |
| **004 weekly-reckoning** | Implemented (T023–T024 open) | **Converged** for everything buildable here — `tasks_appended` for owner and CI items only | `pnpm typecheck` 9/9, `pnpm lint` clean; unit: territory-rules 233, h3-fixtures 12, walk-sim 46, api-schema 3, api 201 (145 DB tests skipped); `test:db`: api **345 passed, 1 skipped** (perf 10 267 cells in 2.0 s); ml 31; Swift Core 79, APIClient 21 (generated from the refreshed snapshot, no generator warnings after the `captain` fix); live A.3 replay: W36 dry-run flips = list owners (9 cells) | T023 owner decisions (push time, flip XP / top 100 / 3 000-cell cap, `admin` user per environment); T024 first CI run (`db:migrate` on the h3-pg image, `SKIP_PERF` split) and Docker `make test` |

Cross-feature facts: `.github/workflows/api.yml` (jobs `rules`, `api`, `swift ×11`) has been parsed with `yq` by every integration stream but never executed — no feature has had an Actions run yet. `packages/api-schema/openapi.json` and its iOS copy are identical and carry every 001–004 operation (17 paths). No territory rule or fixture changed since 001 (`docs/territory-rules.md` "Constants summary" = `packages/territory-rules/config.ts` = `Rules.swift`, pinned by the constants tests).

## Owner-action checklist (consolidated from `specs/00{1,2,3,4}-*/tasks.md`)

| # | Action | Where it is tracked | Unblocks |
|---|---|---|---|
| 1 | Connect the repository to **Xcode Cloud** and run the PR workflow on scheme `NatureExplorer` (`apps/ios/README.md` "Xcode Cloud"); confirm `ci_scripts/ci_post_clone.sh` runs | 001 T051 | app-target and SwiftUI type-checking on every PR (001 T050, 002 T032, 003 T034 can then be closed from the Xcode Cloud log) |
| 2 | Install **git-lfs** on a machine with network, download and commit the model weights under `apps/ios/Resources/Models` as LFS pointers (`python3 ml/scripts/download_models.py --dest apps/ios/Resources/Models`) | 001 T049 | on-device bird capture (006), Xcode Cloud builds with models |
| 3 | Enable **Sign in with Apple** on the App ID `com.natureexplorer.app` in the Apple Developer portal | 002 T033 | real-device sign-in |
| 4 | Set **`DEVELOPMENT_TEAM`** in `apps/ios/project.yml` once the App ID exists | 001 T054 note, 002 T033 | signing, Xcode Cloud |
| 5 | Generate a **`JWT_SECRET`** per API environment (`node -e "console.log(require('crypto').randomBytes(48).toString('base64'))"`, ≥ 32 chars; rotating it signs everyone out) | 002 T033 | any non-dev API deployment (the API refuses to boot without it) |
| 6 | Set **`APPLE_CLIENT_IDS`** (production and TestFlight bundle ids, comma-separated) on every API environment | 002 T033 | identity-token audience check |
| 7 | Write the final **usage-string copy** for `NSLocationWhenInUseUsageDescription` and `NSMotionUsageDescription` in `apps/ios/NatureExplorer/Info.plist` (both end in `TODO(owner): final copy.`) | 003 T031 | first TestFlight build / App Review |
| 8 | Send the **licence inquiry e-mails** (`ml/licensing/plantnet-pro-inquiry.md` before the public TestFlight; optionally `ml/licensing/birdnet-v24-inquiry.md`) and fill the `sent date` cells in `docs/licences.md` | 001 T053 | Constitution III before shipping those models |
| 9 | Run **Docker `make test`** once on a machine with Docker Compose v2 (builds `nature-postgres:16-3.4-h3`, MinIO, `pnpm test:db`; add `S3_TEST=1` for the storage suite) and open the integration PR so the three CI jobs run | 001 T052, 002 T034/T035, 003 T035, 004 T024 | proof that the h3-pg image, migrations 0000–0005 and the workflow run outside the agent container |
| 10 | Create an **`admin` user** per environment for `walk-sim reckon` / `POST /v1/admin/reckonings/{weekId}`: `psql "$DATABASE_URL" -c "update users set role = 'admin' where id = '<user id>'"` (no self-service path by design) | 004 T023 | manual reckonings and the weekly review of Monday's results |
| 11 | Decide the **Monday results push time** (08:00 UTC now; local morning in 008) and confirm **flip XP 15, leaderboard top 100, 3 000-hexagon map cap** before the public TestFlight | 004 T023 (`plan.md` "Owner actions") | 008's push worker; constants frozen for TestFlight |
| 12 | On a device: **battery** of a 1-hour walk (< 6 %/h, SC-006 of 003) and the **SC-003 timings** (summary within 5 s of Stop, offline walk uploaded within 2 min) | 003 T032, T033 | Constitution VII evidence; not CI gates |

When items 1, 4 and 9 are done, re-run `/speckit-converge` for 001 and 002 (T048, T031); they should report Converged.
